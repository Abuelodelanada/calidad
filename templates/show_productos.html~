{% extends "layout.html" %}
{% block body %}
  {% if session.logged_in %}
    <form action="{{ url_for('add_producto') }}" method=post class=add-entry>
      <dl>
        <dt>Nombre:
        <dd><input type=text size=30 name=nombre>
        <dt>Código:
        <dd><input type=text size=30 name=codigo>
        <dd><input class="btn btn-primary" type=submit value=Crear Producto>
      </dl>
    </form>
  {% endif %}
  <ul class=entries>
  {% for producto in productos %}
    <li><h2>{{ producto.nombre }}</h2>{{ producto.codigo }}
  {% else %}
    <li><em>Unbelievable.  No entries here so farx</em>
  {% endfor %}
  </ul>
{% endblock %}
